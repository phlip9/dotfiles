#!/usr/bin/picolisp /usr/lib/picolisp/lib.l

(let N 0
 (in NIL
  (do (dec (format (opt)))
   (inc
    'N
    (if (sub? (prin (char)) "([") 3 1) ) )
  (pretty (read) (- N))
  (echo) ) )
(bye)

# vi:et:ts=3:sw=3
