diff --git a/packages/cli/src/utils/client.ts b/packages/cli/src/utils/client.ts
index 0229798..359c260 100644
--- a/packages/cli/src/utils/client.ts
+++ b/packages/cli/src/utils/client.ts
@@ -1,3 +1,4 @@
+import { readFileSync } from "node:fs";
 import {
   waitForDaemonReady,
   resolvePaseoHome,
@@ -169,7 +170,16 @@ export function resolveDaemonPassword(host: string): string | undefined {
     if (fromUri) return fromUri;
   }
   const fromEnv = process.env.PASEO_PASSWORD;
-  return fromEnv && fromEnv.length > 0 ? fromEnv : undefined;
+  if (fromEnv && fromEnv.length > 0) return fromEnv;
+
+  const passwordFile = process.env.PASEO_PASSWORD_FILE?.trim();
+  if (!passwordFile) return undefined;
+
+  const fromFile = readFileSync(passwordFile, "utf8").trim();
+  if (!fromFile) {
+    throw new Error("PASEO_PASSWORD_FILE is empty");
+  }
+  return fromFile;
 }
 
 /**
diff --git a/packages/server/src/server/config-environment.ts b/packages/server/src/server/config-environment.ts
index 92bf918..e5147b2 100644
--- a/packages/server/src/server/config-environment.ts
+++ b/packages/server/src/server/config-environment.ts
@@ -29,6 +29,7 @@ export const DAEMON_SETTING_ENV_KEYS = [
   "PASEO_LOG_ROTATE_COUNT",
   "PASEO_LOG_ROTATE_SIZE",
   "PASEO_PASSWORD",
+  "PASEO_PASSWORD_FILE",
   "PASEO_RELAY_ENABLED",
   "PASEO_RELAY_ENDPOINT",
   "PASEO_RELAY_PUBLIC_ENDPOINT",
diff --git a/packages/server/src/server/config.ts b/packages/server/src/server/config.ts
index a683ec6..4f7d825 100644
--- a/packages/server/src/server/config.ts
+++ b/packages/server/src/server/config.ts
@@ -1,5 +1,5 @@
 import { configurationEnvironment } from "./config-environment.js";
-import { existsSync } from "node:fs";
+import { existsSync, readFileSync } from "node:fs";
 import path from "node:path";
 import { fileURLToPath } from "node:url";
 import { resolvePaseoNodeEnv } from "./paseo-env.js";
@@ -489,6 +489,14 @@ function resolveAuthConfig(
   if (envPassword) {
     return { password: hashDaemonPassword(envPassword) };
   }
+  const envPasswordFile = env.PASEO_PASSWORD_FILE?.trim();
+  if (envPasswordFile) {
+    const filePassword = readFileSync(envPasswordFile, "utf8").trim();
+    if (!filePassword) {
+      throw new Error("PASEO_PASSWORD_FILE is empty");
+    }
+    return { password: hashDaemonPassword(filePassword) };
+  }
   return persisted.daemon?.auth?.password
     ? { password: persisted.daemon.auth.password }
     : undefined;
