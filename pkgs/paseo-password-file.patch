diff --git a/packages/server/src/server/config.ts b/packages/server/src/server/config.ts
index 5c92cc0..f0b763a 100644
--- a/packages/server/src/server/config.ts
+++ b/packages/server/src/server/config.ts
@@ -1,3 +1,4 @@
+import { readFileSync } from "node:fs";
 import path from "node:path";
 import { fileURLToPath } from "node:url";
 import { resolvePaseoNodeEnv } from "./paseo-env.js";
@@ -373,6 +374,14 @@ function resolveAuthConfig(
   if (envPassword) {
     return { password: hashDaemonPassword(envPassword) };
   }
+  const envPasswordFile = env.PASEO_PASSWORD_FILE?.trim();
+  if (envPasswordFile) {
+    const filePassword = readFileSync(envPasswordFile, "utf8").trim();
+    if (!filePassword) {
+      throw new Error("PASEO_PASSWORD_FILE is empty");
+    }
+    return { password: hashDaemonPassword(filePassword) };
+  }
   return persisted.daemon?.auth?.password
     ? { password: persisted.daemon.auth.password }
     : undefined;
